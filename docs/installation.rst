============
Installation
============

At the command line::

    $ easy_install django-ltree-utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-ltree-utils
    $ pip install django-ltree-utils
